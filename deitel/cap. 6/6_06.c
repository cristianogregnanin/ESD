/* ex. 6.06 Deitel C how to program */
/* Riempite gli spazi in ognuna delle seguenti righe:
*/

// a) Il C immagazzina le liste di valori nei VETTORI

// b) Gli elementi di un vettore sono correlati dal fatto che HANNO LO STESSO TIPO E NOME

// c) Quando si fa riferimento a un elemento di un vettore, il numero di posizione contenuto
//    all'interno delle parentesi quadre è detto INDICE.

// d) I nomi dei cinque elementi del vettore p sono p[0], p[1], p[2], p[3], p[4]

// e) Il contenuto di un particolare elemento di un vettore è il VALORE dell'elemento.

// f) Dare un nome a un vettore, stabilirne il tipo e specificare il numero degli elementi
//    contenuti nello stesso è la DEFINIZIONE di un vettore

// g) Il processo di sistemazione in ordine ascendente o discendente degli elementi di un vettore
//    è chiamato ORDINAMENTO

// h) In una matrice, il primo indice (per convenzione) identifica la RIGA di un elemento,
//    mentre il secondo indice (per convenzione) identifica la COLONNA di un elemento.

// i) Una matrice m per n contine M righe, N colonne e M*N elementi.

// j) Il nome dell'elemento nella riga 3 e nella colonna 5 di un amatrice d è d[3][5]
