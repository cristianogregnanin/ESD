/* ex. 6.02 Deitel C how to program */
/* Stabilite se le seguenti affermazioni siano vere o false. Qualora la risposta sia falsa, spiegatene il motivo
*/

// a) Un vettore può immagazzinare i valori di molti tipi differenti. (F solo di un tipo alla volta)

// b) L'indice di un vettore può essere del tipo dato double. (F può essere solo intero)

// c) Nel caso che in una lista di inizializzatori ci siano meno elementi di quelli presenti nel
//    vettore, il C inizializzerebbe automaticamente quelli rimanenti utilizzando l'ultimo valore
//    incontrato nella lista degli inizializzatori. (F li inizializza a 0)

// d) E' un errore inserire in una lista di inizializzatori più elementi di quanti siano presenti nel vettore (V)

// e) Qualora un singolo elemento di un vettore sia passato a una funzione e sia modificato dalle stessa,
//    nella funzione chiamante quell'elemento conterrà il valore modificato. (F viene modificato il vettore originale solo se
//    viene passato nella funzione l'intero vettore)
