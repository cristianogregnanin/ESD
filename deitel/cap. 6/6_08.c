/* ex. 6.08 Deitel C how to program */
/* Scrivete delle istruzioni che eseguano ognuna delle seguenti attività:
*/

// a) Visualizzate il valore contenuto nel settimo elemento del vettore di caratteri f:

//    printf(%c\n",f[6]);

// b) Prendete in input un valore e sistematelo nell'elemento 4 del vettore unidimensionale di
//    valori in virgola mobile b.

//   scanf("%d", &f[6] );

// c) Inizializzate a 8 ognuno dei 5 elementi interi contenuti nel vettore unidimensionale g.

/*    for (i=0; i<=5, i++){
        g[i]=8;
      }                   */

// d) Sommate i 100 elementi contenuti nel vettore di valori in virgola mobile c.

/*    for(i =0; i<=100; i++){
        somma += c[i];
      }                   */

// e) Copiate il vettore a nella prima porzione del vettore b. Assumete che sia già stata scritta la
//    dichiarazione double a[11], b[34]

/*   for(i=0; i<=11; i++){
        b[i] = a[i];
      }                     */

// f) Determinate e visualizzate il valore più piccolo e quello più grande contenuti nel vettore w
//    di 99 elementi in virgola mobile.

/*   small = w[0];
     large = w[0];

     for (i=1; i<=98; i++){
          if(w[i] < small)
            small = w[i];
          else if (w[i] > large)
            large = w[i];
      }                            */
