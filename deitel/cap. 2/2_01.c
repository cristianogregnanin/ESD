/* ex_2.1 deitel*/

/*

a) Ogni programma C incomincia la propria esecuzione con la funzione Main().
b) La '{' inizia il corpo di ogni funzione mentre la '}' termina il corpo di ogni funzione.
c) Ogni istruzione termina con ';'.
d) La funzione printf della libreria standard visualizza le informazioni sullo schermo.
e) la sequenza d'escape \n rappresenta il carattere 'new line' che muove il cursore
   nella posizione iniziale della riga successiva sullo schermo.
f) La funzione scanf della libreria standard è utilizzata per leggere i dati da tastiera.
g) La specifica di conversione '%d' è usata in una stringa di controllo del formato di
   una funzione scanf, per indicare che sarà immesso un intero, mentre in una stringa di
   controllo del formato di una funzione printf, per indicare che sarà visuallizato un intero.
h) Ogniqualvolta un nuovo valore sarà sistemato in una locazione di memoria, esso si sostituirà
   al valore contenuto in precedenza in quella locazione. Questo processo è noto come scrittura distruttiva.
i) Quando un valore sarà letto da una locazione di memoria, il valore in quella locazione sarà
   preservato; questo processo è detto lettura non distruttiva.
j) L'istruzione if è utilizzata per prendere delle decisioni.

*/
