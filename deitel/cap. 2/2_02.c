/* ex_2.2 deitel*/

/*

a) Quando sarà invocata la funzione printf, questa comincerà a visualizzare sempre
   dall'inizio di una nuova riga. (F, inizia dove è finita la prima)

b) I commenti inducono il computer a visualizzare sullo schermo il testo tra / * e * /
   durante l'esecuzione del programma. (F. i commenti vengono ignorati)

c) La sequenza di escape \n, quando è usata in una stringa di controllo del formato di una
   funzione printf, induce il cursore a posizionarsi all'inizio della riga successiva sullo schermo.
   (V)

d) Tutte le variabili devono essere dichiarate prima di essere utilizzate. (V)

e) A tutte le variabili dovrà essere assegnato un tipo, quando saranno dichiarate. (V)

f) Il C considera identiche le variabili number e NuMbEr. (F, C è case sensitive)

g) Le dichiarazioni possono apparire in qualsiasi parte del corpo di una funzione.
   (F, prima di utilizzare la variabile)

h) Tutti gli argomenti successivi alla stringa di controllo del formato in una funzione printf
   devono essere preceduti da un ampersand (&) (F, a scanf)

i) L'operatore modulo (%) può essere utilizzato soltanto con degli operandi interi (V)

j) Gli operatori aritmetici *, /, %, + e - hanno tutti lo stesso livello di priorità. (F)

k) I seguenti nomi di variabili sono identici in tutti i sistemi ANSI C.
   thisisasuperlongname1234567
   thisisasuperlongname1234568
   (F perchè alcuni identificatori riescono a distinguere più di 31 caratteri)

l) Un programma che visualizzi tre righe di output dovrà contenere tre istruizioni printf.
   (F, basta mettere i caretteri new_line)
*/
