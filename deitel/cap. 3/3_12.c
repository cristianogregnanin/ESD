/* ex_3.12 deitel*/

/* Riempite gli spazi in ognuna delle frasi seguenti: */

/*a*/ La soluzione di ogni propblema richiede l'esecuzione di una serie di azioni in un ORDINE specifico.

/*b*/ Un sinonimo di procedura è ALGORITMO

/*c*/ Una variabile che accumola la somma di diversi numeri è un TOTALE

/*d*/ Il processo in cui si impostano certe variabili con dei valori specifici all'inizio di un
      programma è detto INIZIALIZZAZIONE

/*e*/ Un valore speciale utilizzato per indicare la "fine della immissione dei dati" è detto valore
      SENTINELLA, SEGNALE, DUMMY o FLAG;

/*f*/ Un DIAGRAMMA DI FLUSSO è la rappresentazione grafica di un algoritmo

/*g*/ In un diagramma di flusso, l'ordine in cui dovranno essere eseguiti i passi sarà indicato dai
      simboli di LINEE DI FLUSSO

/*h*/ Il simbolo di terminazione indica l'INIZIO e FINE di ogni algoritmo

/*i*/ Il simbolo rettangolo corrisponde ai calcoli, che normalmente saranno eseguiti dalle
      istruzioni DI CALCOLO, e alle operazioni di input/output che normalmente saranno eseguite
      dalle chiamate alle funzioni PRINTF e SCANF della libreria standard.

/*j*/ L'elemento scritto all'interno di un simbolo di decisione è detto CONDIZIONE.
