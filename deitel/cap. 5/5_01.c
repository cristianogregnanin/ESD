/* ex. 5.01 Deitel C how to program */
/* rispondete ad ognuna delle seguenti domande: */


/*a*/ Un modulo di un programma in C è una FUNZIONE.

/*b*/ Una funzione è invocata con una CHIAMATA DI FUNZIONE.

/*c*/ Una variabile che sia nota solo all'interno della funzione in cui è stata definita è una
      VARIABILE LOCALE.

/*d*/ L'istruzione RETURN di una funzione chiamata sarà utilizzata per restituire a quella
      chiamante il valore di una espressione.

/*e*/ La parola chiave VOID sarà utilizzata nell'intestazione di una funzione, per indicare
      che non restituirà un valore o che non conterrà dei parametri.

/*f*/ La VISIBILITA' di un identificatore è la porzione del programma in cui quello potrà essere
      utilizzato.

/*g*/ I tre modi per restituire il controllo da una funzione chiamata al chiamante sono RETURN,
      RETURN + ESPRESSIONE e CON UNA PARENTISI GRAFFA DI CHIUSURA DELLA FUNZIONE.

/*h*/ Un PROTOTIPO DI FUNZIONE consente al compilatore di verificare il numero, i tipi e l'ordine degli
      argomenti passati a una funzione.

/*i*/ La funzione RAND() è utilizzara per generare dei numeri casuali.

/*j*/ La funzione SRAND() è utilizzata per impostare il seme del numero casuale, in modo
      da randomizzare un programma.

/*k*/ Le specifiche di classe di memoria sono AUTO, REGISTER, EXTERN e STATIC.

/*l*/ Le variabili dichiarate in un blocco o nella lista dei parametri di una funzione, sempre che
      non sia stato specificato diversamente, saranno considerate della classe di memoria AUTO.

/*m*/ La specifica di classe di memoria REGISTER è una raccomandazione rivolta al compilatore
      affinchè immagazzini una variabilie in uno dei registri del computer.

/*n*/ Una variabile dichiarata all'esterno di qualsiasi blocco o funzione è una variabile GLOBALE.

/*o*/ Perchè una variabile locale di una funzione possa conservare il prorpio valore tra le diversamente
      chiamate alla funzione stessa, essa dovrà essere dichiarata con la specifica di classe di
      memoria STATIC.

/*p*/ Le quattro possibili visibilità per un identificatore sono VISIBILITA NELLA FUNZIONE, NEL FILE, NEL BLOCCO e
      NEL PROTOTIPO DI FUNZIONE.

/*q*/ Una funzione che richiama se stessa in modo diretto o indiretto è una funzione RICORSIVA.

/*r*/ Una funzione ricorsiva avrà tipicamente due parti: una che fornisca un mezzo per terminare
      la ricorsione, verificando l'occorrenza di un caso DI BASE, e l'altra che esprima il
      problema con una chiamata ricorsiva a uno leggermente più semplice di quello della
      chiamata originaria.
