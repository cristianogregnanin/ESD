/* ex. 5.48 Deitel C how to program */
/* Trovate l'errore in ognuno dei seguenti segmenti di programma e spiagate come correggerlo
  */

/* a) double cube(float number); // prototipo di funzione

      // ....

      double cube (float number) // definizione di funzione
      {
        return number * number * number;
      }          */

// b) register int x = 7;

// c) int randomNumber = rand();

/* d) double y = 123.45678;
      double x;
      x = y;
      printf("%f\n", (double) x ); */

/* e) double square (double number)
      {
        return number * number;
      }   */

/* f) int sum (int n)
      {
        if (n == 0)
            return 0;
        else
          return n + sum(n - 1);
      }       */
