/* ex. 5.04 Deitel C how to program */
/* Fornite l'intestazione per ognuna delle seguenti funzioni:

a) La funzione hypotenuse che riceva due argomenti in virgola mobile a doppia precisione,
   side1 e side2, e restituisca un risultato in virgola mobile a doppia precisione.

   double hypotenuse (double side1, double side2)

b) La funzione smallest che prenda tre interi, x, y e z, e restituisca un intero.

   int smallest (int x, int y, int z)

c) La funzione instructions che non riceva alcun argomento, nè restituisca un valore.

   void instructions (void)

d) La funzione intToFloat che prenda un argomento intero, number, e restituisca un risultato
   in virgola mobile.

   float intToFloat (int number)
*/
